<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".add.AddFragment">


    <EditText
        android:id="@+id/edt_text_title"
        android:layout_width="0dp"
        android:layout_height="@dimen/_60dp"
        android:layout_marginStart="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="16dp"
        android:ems="10"
        android:inputType="textPersonName"
        android:minHeight="48dp"
        android:hint="Title"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Spinner
        android:id="@+id/spinner_prioritas"
        android:layout_width="0dp"
        android:entries="@array/prioritas"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:minHeight="48dp"
        android:paddingEnd="@dimen/_20dp"
        app:layout_constraintEnd_toEndOf="@+id/edt_text_title"
        app:layout_constraintStart_toStartOf="@+id/edt_text_title"
        app:layout_constraintTop_toBottomOf="@+id/edt_text_title"
        tools:ignore="SpeakableTextPresentCheck" />

    <EditText
        android:id="@+id/edt_txt_desc"
        android:layout_width="0dp"
        android:layout_height="@dimen/_60dp"
        android:layout_marginTop="16dp"
        android:ems="10"
        android:inputType="textMultiLine"
        android:hint="Name"
        android:minHeight="48dp"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingHorizontal="16dp"
        app:layout_constraintEnd_toEndOf="@+id/spinner_prioritas"
        app:layout_constraintStart_toStartOf="@+id/spinner_prioritas"
        app:layout_constraintTop_toBottomOf="@+id/spinner_prioritas" />
</androidx.constraintlayout.widget.ConstraintLayout>